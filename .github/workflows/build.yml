name: Build & Release BakaPlus

on:
  push:
    branches:
      - 'main'
      - 'master'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Restore google-services.json
        env:
          GOOGLE_SERVICES_JSON: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: |
          echo "$GOOGLE_SERVICES_JSON" | base64 -d > app/google-services.json

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Build App
        run: |
          ./gradlew :app:assembleRelease --stacktrace

      - name: Sign App
        id: sign_app
        uses: ilharp/sign-android-release@v2.0.0
        with:
          releaseDir: app/build/outputs/apk/release
          signingKey: ${{ secrets.KEYSTORE_BASE64 }}
          keyAlias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Rename APK
        id: rename_apk
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          GRADLE_VER=$(grep -E 'versionName\s*=' app/build.gradle.kts | head -n 1 | sed -E 's/.*"([^"]+)".*/\1/')
          VERSION="v${GRADLE_VER:-1.0}"

          TARGET_FILE=$(find app/build/outputs/apk/release/ -type f \( -name "*-signed.apk" -o -name "*.apk" \) | head -n 1)

          if [ -n "$TARGET_FILE" ] && [ -f "$TARGET_FILE" ]; then
            NEW_NAME="BakaPlus-${VERSION}-${SHORT_SHA}.apk"
            NEW_PATH="app/build/outputs/apk/release/${NEW_NAME}"
            mv "$TARGET_FILE" "$NEW_PATH"
            echo "apk_path=${NEW_PATH}" >> $GITHUB_OUTPUT
            echo "apk_name=${NEW_NAME}" >> $GITHUB_OUTPUT
            echo "tag_name=${VERSION}-${SHORT_SHA}" >> $GITHUB_OUTPUT
            echo "release_name=Release ${VERSION} (${SHORT_SHA})" >> $GITHUB_OUTPUT
          fi

      - name: Build Changelog
        id: changelog
        uses: ardalanamini/auto-changelog@v5.0.0
        with:
          mention-authors: false
          mention-new-contributors: false
          include-compare: false
          semver: false

      - name: Create Release
        uses: ncipollo/release-action@v1.21.0
        with:
          artifacts: ${{ steps.rename_apk.outputs.apk_path }}
          body: ${{ steps.changelog.outputs.changelog }}
          tag: ${{ steps.rename_apk.outputs.tag_name }}
          name: ${{ steps.rename_apk.outputs.release_name }}